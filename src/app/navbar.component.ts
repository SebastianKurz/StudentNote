import { Component } from '@angular/core';

@Component({
  selector: 'navbar',
  template: `Home | School | Classes | Teacher | Students                       | Logoff `
})

export class NavbarComponent {
}
